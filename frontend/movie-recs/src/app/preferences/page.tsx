'use client';

const Preferences = ({ login = false }:{ login: boolean }) => {
    return(
        <div>
            <h1>Preferences</h1>
            
        </div>
    );
}

export default Preferences;